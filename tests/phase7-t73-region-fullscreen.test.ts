/**
 * Phase 7 / T7.3 — 区域框选与全屏截图模式（Post-MVP）
 *
 * 目标（docs/tasks.md T7.3）：手动区域框选截图（Region Shot）与全屏截图
 * （Screen Shot）模式。
 *
 * 验收要点（落地后转为可执行测试）：
 * - 区域模式：框选矩形与输出图片像素一致（Retina 坐标换算正确）；
 * - 全屏模式：多显示器下按目标屏幕捕获，不跨屏拼图。
 */
import { test } from 'node:test'

test('T7.3 区域框选与全屏截图模式（Post-MVP 占位）', {
  skip: 'Post-MVP（Phase 7）暂无实现；落地时覆盖：区域框选坐标→像素一致、全屏模式按目标屏幕捕获',
}, () => {})
